type Vec3 struct {
    x: f32,
    y: f32,
    z: f32
}

fn main() {
    v: Vec3;
    fv := cast(*f32) &v;

    ^(fv + 1) = 1.7;

    ret v.y;
}

fn assert(b: bool) {
    if (b == false) {
        panic();
    }
    ret 0;
}
