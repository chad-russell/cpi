fn id(T : Type)(a: T) { ret a.x; }

type Foo struct { x: i64, y: i64 }
type Bar struct { z: i64, x: i64 }

fn main() {
    f: Foo;
    f.x = 99;
    f.y = 14;

    b: Bar;
    b.z = 88;
    b.x = 88;

    ret id!(Foo)(f) + id!(Bar)(b);
}
