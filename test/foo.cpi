fn main() {
    #import "textedit";
    textedit.example();

    -- type Foo struct {
        -- x: *none
    -- }

    -- #import "io";
    -- #import "mem";

    -- f: Foo;

    -- f.x = mem.malloc(1);
    -- io.println(f.x);

    -- io.println(cast(*i8) f.x);

    return 0;
}
