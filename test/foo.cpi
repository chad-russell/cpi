fn main() {
    sum := 0;

    for e: []{1, 2, 3} {
        sum = sum + e;
    }

    ret sum;
}

fn array_for_add() {
    for e: []{1, 2, 3} {
    }

    ret 666;
}

fn array_while_add() {
    arr := []{1, 2, 3};
    i := 0;
    while i < arr.count {
        e := arr[i];
    }

    ret 666;
}
