type Context struct {
    id: i64
}

fn bar() {
    return 3;
}

fn foo() {
    return bar;
}

fn main() {
    f := fn() {
        return fn() {
            return foo;
        };
    };

    assert(3 == f()()()());

    return 0;
}

fn assert(b: bool) {
    if (b == false) { panic(); }
}
